<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Maalem">

        <!-- Auth : activité principale (launcher) -->
        <activity
            android:name=".presentation.auth.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Citoyen -->
        <activity
            android:name=".presentation.citizen.CitizenHomeActivity"
            android:exported="false" />

        <!-- Artisan -->
        <activity
            android:name=".presentation.artisan.ArtisanHomeActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.artisan.PendingValidationActivity"
            android:exported="false" />

        <!-- Admin -->
        <activity
            android:name=".presentation.admin.AdminHomeActivity"
            android:exported="false" />

        <!-- Main (si utilisé) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>

</manifest>